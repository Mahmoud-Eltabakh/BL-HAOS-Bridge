[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
addopts = -v --strict-markers -p asyncio
asyncio_mode = auto
markers =
    asyncio: async test marker (pytest-asyncio)
filterwarnings =
    error::RuntimeWarning:pytest\.stash




